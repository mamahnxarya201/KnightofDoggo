extends YSort


# Declare member variables here. Examples:
# var a = 2
# var b = "text"


# Called when the node enters the scene tree for the first time.
func _ready():
#	var list_bat = get_tree().get_nodes_in_group("Bat_Enemy")
#	for bat in list_bat:
#		bat.connect("killed", $YSort/Player, "_on_Bat_killed")
#		print(is_connected("killed", $YSort/Player, "_on_Bat_killed"))
	pass


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
